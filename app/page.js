// Removed unused import

export default function Home() {
  // Removed unused variable

  [1, 2, 3].forEach((item) => {
    // This is a dummy function to demonstrate the removal of unused imports
    console.log(item);
  });

  return (
    <div>
      Hello world
      <button>button added</button>
    </div>
  );
}
